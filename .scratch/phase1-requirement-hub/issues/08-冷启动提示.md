# 08 工单 /implement 技能冷启动提示（Phase 1 回归验证）

> 本文件是给**全新会话**使用的自包含 prompt。粘贴下方"=== 冷启动提示 ==="之间的内容即可启动 implement 技能实施 08 工单。
> 工单原文：`.scratch/phase1-requirement-hub/issues/08-Phase1回归验证.md`

---

## === 冷启动提示 ===

请用 `implement` 技能实施 **08 工单 — Phase 1 回归验证**。

工单文件路径：`.scratch/phase1-requirement-hub/issues/08-Phase1回归验证.md`（请先完整读一遍）。

### 一、本工单在 Phase 1 中的定位（一句话）

08 是 Phase 1 上线前的**最后一道质量门禁**：在集成分支上验证 3 个纯函数单测齐全、新增 4 个集成测试覆盖跨用户隔离与数据流链路、跑 v1 三模块 AI 评测集回归 + v1 RLS 集成回归，确认 03/04/05/06/07 的叠加改动未引入质量回退。**08 不写新功能代码，只写测试与验证脚本扩展。**

### 二、关键背景（已核实，请直接采信，勿重复调研）

#### 2.1 集成分支挑战（本工单最大难点，务必先处理）

Phase 1 七个工单的分支拓扑如下（已用 `git log --graph` 核实）：

```
main (01+02)
  └─ feat/03-requirement-crud-filter        (03 Requirement CRUD)
       └─ feat/04-confirm-gate              (04 Confirm 关卡)
            ├─ feat/05-requirement-to-api   (05 Requirement→API)
            │    └─ feat/07-api-grouped-view (07 API 分组视图，含 05 全部代码)
            └─ feat/06-meeting-to-feedback  (06 Meeting→Feedback，与 05 是兄弟分支)
```

**关键事实：05 和 06 是兄弟分支，互不包含对方的代码。07 从 05 分叉，所以 feat/07 含 05+07 但不含 06。没有任何一个现有分支同时含 05+06+07。**

08 工单 `Blocked by: 05, 06`，因此必须在**集成分支**上工作，该分支需先把 06 合进来：

```bash
# 第一步：从 feat/07（已含 05+07）切出集成分支
git checkout feat/07-api-grouped-view
git checkout -b feat/08-regression

# 第二步：把 feat/06 合进来（05 与 06 改动文件基本不重叠：05 改 api-designer，06 改 meetings/feedback，预期无冲突或少量冲突）
git merge feat/06-meeting-to-feedback
# 若有冲突：以"两边都要"的原则手工合并（05 的 source_requirement_id 接线 + 06 的 transfer action 都要保留）
```

合并后该分支应同时含：03 CRUD + 04 lifecycle 关卡 + 05 requirement→API 接线 + 06 meeting→feedback 转入 + 07 分组视图。先跑 `npm run typecheck` 确认合并干净，再开始写测试。

#### 2.2 三个纯函数单测【早已存在】，08 只验证不改写

工单前三个 checklist 项对应的单测**已在 03/04/07 工单中写完**，08 的任务是"在集成分支上跑一遍确认全绿"，**不是重新写**。已核实位置与覆盖度：

| Checklist 项 | 测试文件（集成分支上） | 现有覆盖 |
|---|---|---|
| requirements 校验纯函数 | `src/lib/__tests__/requirements.test.ts` → `describe("validateRequirementInput")` | 13 个 it：空标题/纯空白/非字符串/超 200 字/刚好 200 字/空内容/纯空白内容/非字符串内容/非法 priority 归一化/合法 priority/默认 medium/lifecycle 恒 draft |
| canTransition 单测 | 同文件 → `describe("canTransition …")` + `describe("describeTransition …")` | 21 个 it：draft→confirmed / draft→parked / confirmed→in_progress / confirmed→parked / in_progress→delivered / in_progress→parked 全部 true；delivered→任意 / parked→任意 / confirmed→draft 回退 / 跨级跳跃 / 自环 全部 false；describeTransition 合法/非法文案 |
| groupApiDraftsByRequirement 单测 | `src/lib/__tests__/api-designer.test.ts` → `describe("groupApiDraftsByRequirement")` | 9 处匹配（含 有归属/无归属/多接口同需求/单接口单需求/孤儿 source_requirement_id 归"未归属"） |

**08 对这三项的动作：** `npm test` 跑一遍，确认在集成分支上 100% 绿。若发现某边界缺失（极少见），才补 case；否则**不动测试代码**。

#### 2.3 四个集成测试【是本工单真正的新工作】

现有 `scripts/integration-check.mjs` 含 10 个 check（baseline 10/10，调 Supabase REST，service_role 建数据 + anon 模拟用户 A/B 验证 RLS 隔离，不依赖 dev server）。08 需**扩展该脚本**，新增 4 个 check（编号 11-14），沿用现有 `check()` 模式：

| 新 Check | 验证点 | 实现要点 |
|---|---|---|
| 11 Requirement CRUD 跨用户 RLS 隔离 | A 建需求，B 看不到/改不了/删不了 | service_role 给 A 建一条 requirement；anon-A select 能看到；anon-B select 返回空；anon-B update/delete 返回错误或 0 行影响 |
| 12 会议 issue 转入后 feedback_items 带 source_meta + 聚类触发 + 防重复 | 转入链路数据正确性 | 触发 06 的 transfer 链路（或直接 service_role 模拟写入 feedback_items 带 `source_type='meeting'` + `source_meta={meeting_id, meeting_item_id}`）；验证 source_meta 字段存在；重复转入同一条返回防重复错误 |
| 13 需求→API 带入后 source_requirement_id 正确 + 越权防护 | 带入链路数据正确性 | service_role 给 A 建一条 confirmed requirement；模拟 createApiDraft 传 sourceRequirementId；验证 api_drafts.source_requirement_id 落库；B 的 requirement id 传给 A 应被拒（越权防护） |
| 14 软删后列表不展示 + 详情页直链处理 | deleted_at 语义 | service_role 给 A 建需求并 soft-delete（deleted_at 非空）；anon-A 列表查询（`deleted_at IS NULL` 过滤）不返回；详情直链查询可返回（详情页需处理"已删除"态） |

**集成测试的执行依赖**：需 `.env.local` 含 `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY`，且 Supabase 项目里有两个测试用户 A/B（v1 集成脚本已假设其存在）。

#### 2.4 两个回归（跑现有脚本，不改脚本逻辑）

| 回归 | 命令 | 门槛 | 说明 |
|---|---|---|---|
| v1 三模块 AI 评测集 | `node scripts/run-eval.mjs` | 会议提取/反馈聚类/API 生成三模块**程序化项 100%**（人工项仅记录） | 消费 `DASHSCOPE_API_KEY` 环境变量；读 `tests/eval/{api,feedback,meeting}_eval.json`；调真实 DashScope；不在 CI 跑 |
| v1 RLS 集成 | `node scripts/integration-check.mjs` | 旧 10 个 check **仍 10/10** | 加了 4 个新 check 后应为 14/14 |

### 三、实施范围（in scope）

1. **集成分支创建**：`feat/08-regression`（从 feat/07 切，merge feat/06）
2. **纯函数单测验证**：`npm test` 全绿（不写新 case，除非发现缺口）
3. **集成测试扩展**：`scripts/integration-check.mjs` 新增 4 个 check（11-14），沿用现有 `check()` 模式
4. **v1 评测集回归**：`node scripts/run-eval.mjs`，记录三模块结果
5. **v1 RLS 回归**：`node scripts/integration-check.mjs`，14/14
6. **回归报告**：在 `.scratch/phase1-requirement-hub/issues/08-回归报告.md` 记录各项结果（命令、输出摘要、是否达标）

### 四、不在范围内（out of scope）

- ❌ **不改 feature 分支代码**：08 只在集成分支上验证，不回头改 05/06/07 的实现。若回归发现 bug，**记录在回归报告里并新开工单**，不在 08 内修。
- ❌ **不新增纯函数**：纯函数由 03/04/07 写，08 只验证覆盖度。
- ❌ **不接入 CI**：run-eval.mjs 仍手动/里程碑跑（消费真实 DashScope token，成本与稳定性都不适合 CI）。
- ❌ **不写 server action 单测**：action 层不单测，靠 RLS + 集成测试把关（与 03 一致约定）。
- ❌ **不做性能/负载测试**：Phase 1 不含。
- ❌ **不合并到 main**：集成分支 `feat/08-regression` 也不 push、不开 PR（与 01-07 一致）。

### 五、验收门槛（全部满足才算 done）

- [ ] `feat/08-regression` 分支含 05+06+07 全部代码，`npm run typecheck` 绿
- [ ] `npm test` 绿（3 个纯函数 describe block 全过）
- [ ] `node scripts/integration-check.mjs` 输出 **14/14**（旧 10 + 新 4）
- [ ] `node scripts/run-eval.mjs` 三模块程序化项达标（无回退）
- [ ] `.scratch/phase1-requirement-hub/issues/08-回归报告.md` 已写，含每项命令与结果摘要
- [ ] 工单文件 `08-Phase1回归验证.md` 9 个 checklist 全部 `[x]`，Status 改 `done`，附 Branch + Note 行（见下）

### 六、收尾约定（与 03/04/05/06/07 一致）

完成后在 `08-Phase1回归验证.md` 顶部追加：

```
**Status:** done
**Branch:** feat/08-regression (commit <hash>)
**Note:** 分支未 push——按约定只 commit 到当前分支，未做 git push，也未开 PR。合并时需先合 03（feat/03-requirement-crud-filter）再合 04，否则 04 的 PR diff 会带上 03+#02 的 commit。集成分支 feat/08-regression 是 05+06+07 的首次汇合点，合并顺序待定（建议作为最后一个合入 main 的分支，或拆成 05/06/07 分别合）。集成验证（server action / UI 交互 / 埋点实际上报）在本工单完成首次全链路验证。
```

### 七、工作方式建议（切片）

1. **切片 1：集成分支**（30 min）— 切 feat/08-regression，merge feat/06，解冲突，`npm run typecheck` 绿，`npm test` 绿（确认 3 个纯函数 describe 在合并后仍全过）
2. **切片 2：集成测试扩展**（核心，2-3h）— 读现有 integration-check.mjs 的 `check()` 模式，逐个加 check 11-14，每加一个跑一次脚本验证
3. **切片 3：v1 RLS 回归**（10 min）— `node scripts/integration-check.mjs`，确认 14/14
4. **切片 4：v1 评测集回归**（30-60 min，取决于 DashScope 响应）— `node scripts/run-eval.mjs`，记录三模块结果
5. **切片 5：回归报告 + 工单收尾**（20 min）— 写 08-回归报告.md，勾 checklist，改 Status

### 八、预读文档（实施前应熟悉）

- `.scratch/phase1-requirement-hub/issues/08-Phase1回归验证.md`（本工单原文）
- `scripts/integration-check.mjs`（要扩展的脚本，重点看 `check()` 模式与现有 10 个 check 的写法）
- `scripts/run-eval.mjs`（回归脚本，了解输出格式与门槛判定）
- `src/lib/__tests__/requirements.test.ts`（确认 validateRequirementInput + canTransition 覆盖）
- `src/lib/__tests__/api-designer.test.ts`（确认 groupApiDraftsByRequirement 覆盖）
- `CONTEXT.md`（领域上下文）
- `docs/adr/0002-meeting-to-feedback-copy-snapshot.md`（06 转入语义，check 12 依赖）

## === 冷启动提示结束 ===

---

## 附：本工单与前序工单的依赖关系图

```
01 数据地基 ──┐
              ├─→ main ──→ 03 ──→ 04 ──┬─→ 05 ──→ 07 ──┐
02 v1 修复 ──┘                          └─→ 06 ────────┴─→ 08（集成分支）
                                                          ↑
                                          08 = 05+06+07 首次汇合
```

08 完成后，Phase 1 仅剩 09（灰度开关），即可收口。
