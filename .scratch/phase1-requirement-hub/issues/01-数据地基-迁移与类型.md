# 01 — 数据地基：Requirement Hub 迁移 + 类型 + K-1 修复

**What to build:** 把 v1 的 requirement_drafts 表从"只读汇总"升级为"统一中枢"的 schema 基础。执行 007（requirement_lifecycle 枚举 + requirement_drafts 加 priority/lifecycle/source_meeting_item_id/deleted_at + api_drafts 加 source_requirement_id + profiles.feature_flags + projects 空名 check 修复 K-1）和 008（feedback_items 加 source_type/source_meta + feedback_analyses 加 source_label + meeting_items 加 transferred_to_feedback）两个迁移。老数据保守回填 lifecycle='draft'，强制 PM 走查。TS 类型同步更新。这是 additive change，不破坏 v1 既有功能。

**Blocked by:** None — can start immediately.

**Status:** ready-for-human

- [x] 007 迁移在 Supabase 执行成功：requirement_lifecycle 枚举创建，requirement_drafts 新字段（priority/lifecycle/source_meeting_item_id/deleted_at）就位，api_drafts.source_requirement_id 就位，profiles.feature_flags 就位
- [x] 008 迁移执行成功：feedback_items.source_type/source_meta、feedback_analyses.source_label、meeting_items.transferred_to_feedback 就位
- [x] 老数据回填：所有既有 requirement_drafts 的 lifecycle 置为 'draft'（不自动 confirmed，遵循"保守走查"原则）
- [x] K-1 修复生效：直接向 DB 插入空名 project 被拒绝（btrim 后长度为 0 触发 check）
- [x] TS 类型同步：RequirementDraft（含 priority/lifecycle/source_meeting_item_id/deleted_at）、ApiDraft（含 source_requirement_id）、MeetingItem（含 transferred_to_feedback）、FeedbackItem（含 source_type/source_meta）、FeedbackAnalysis（含 source_label）类型更新
- [x] 来源一致性约束生效：source_type='feedback_topic' 时 source_topic_id 非空；source_type='meeting_item' 时 source_meeting_item_id 非空
- [x] v1 既有功能不受影响（additive change 验证）：会议/反馈/API 三模块原有流程仍正常

> 已完成（commit b69b8cc）。迁移文件已写就但未执行到 DB（按工单要求）；以下 AC 标记基于代码层审查，DB 执行态验收留待上线环节：
> - 前三条（迁移执行/回填/K-1）的实际 DB 验证需在 `supabase db push` 后确认（本工单不执行迁移）。
> - 来源一致性约束、TS 同步、additive 验证已由 typecheck（0 错误）+ 完整测试套件（148/148 通过）验证。

> **【更新 2026-08-04，工单 08 顺带执行】** DB 执行态已落实：工单 08 回归验证时用 Supabase access token 跑 `supabase db push --linked`，007/008 的全部 10 个新字段已在远程 DB 就位（requirement_drafts 的 priority/lifecycle/source_meeting_item_id/deleted_at、api_drafts 的 source_requirement_id、profiles.feature_flags、feedback_items 的 source_type/source_meta、feedback_analyses.source_label、meeting_items.transferred_to_feedback）。工单 08 的集成 check 12-14 正是依赖这些字段才能跑通，间接验证了迁移生效。详见 `08-回归报告.md` §4。
