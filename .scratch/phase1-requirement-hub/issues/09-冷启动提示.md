# 09 工单 /implement 技能冷启动提示（Phase 1 灰度开关与上线就绪）

> 本文件是给**全新会话**使用的自包含 prompt。粘贴下方"=== 冷启动提示 ==="之间的内容即可启动 implement 技能实施 09 工单。
> 工单原文：`.scratch/phase1-requirement-hub/issues/09-Phase1灰度开关与上线就绪.md`

---

## === 冷启动提示 ===

请用 `implement` 技能实施 **09 工单 — Phase 1 灰度开关与上线就绪**。

工单文件路径：`.scratch/phase1-requirement-hub/issues/09-Phase1灰度开关与上线就绪.md`（请先完整读一遍）。

### 一、本工单在 Phase 1 中的定位（一句话）

09 是 Phase 1 的**收口工单**：基于 `profiles.feature_flags` 给 Phase 1 新功能（需求模块 / 确认关卡 / 会议转入 / 需求带入 API）加灰度开关——前端控制入口渲染、server action 二次校验防绕过；再走完上线 checklist 的可本地验证项，产出"上线就绪报告 + 上线操作手册"。**09 本身不做实际部署**（push/merge/Vercel 操作按约定留给用户，见 out-of-scope）。

### 二、关键背景（已核实，请直接采信，勿重复调研）

#### 2.1 分支起点（务必从这里开始）

08 已完成，`feat/08-regression` 是 05+06+07+08 的**唯一汇合分支**（已核实包含 06 与 07，merge commit 4bebd1b）。09 依赖全部前序代码，因此：

```bash
git checkout feat/08-regression
git checkout -b feat/09-feature-flag-release
```

当前工作区就在 feat/08-regression 上，可直接切。**不要从 main 或其他 feature 分支切**（main 只有 01+02）。

#### 2.2 feature_flags 现状：schema 已就位，但应用侧是全新 seam

- **DB 列已存在且已生效**：`007_v2_requirement_hub.sql` 第 61 行 `alter table profiles add column feature_flags jsonb not null default '{}'::jsonb;`，08 已通过 `supabase db push --linked` 把 007/008 推到远程 DB——**无需再动迁移**。
- **profiles 表结构**（002_tables.sql）：`id (uuid → auth.users, 注册触发器自动建行)`、`email (unique)`、`display_name`、`created_at`、`updated_at`，加 007 的 `feature_flags`。
- **两个零起点事实**（已 grep 核实）：`src/types/database.ts` 里**没有任何 Profile 类型**；`src/` 里**没有任何 `from("profiles")` 调用**。整个灰度读取链路（类型 + 纯函数 + 读 helper）都是 09 要新建的。

#### 2.3 灰度入口盘点（前端渲染控制点）

| # | 入口 | 位置 | gate 方式 |
|---|---|---|---|
| 1 | 侧栏"需求"模块入口 | `src/components/dashboard/sidebar.tsx`（client 组件，静态 `NAV_ITEMS` 数组，目前无 props） | layout 查 flags → DashboardShell 传 prop → sidebar 按 prop 过滤 NAV_ITEMS |
| 2 | /dashboard/requirements 全部页面（列表/新建/详情） | `src/app/dashboard/requirements/`（Server Components） | 页面级 server 端检查，flag off 渲染"功能尚未开放"占位 |
| 3 | 会议条目"转入反馈"按钮（06） | `src/app/dashboard/meetings/` 详情页 → `MeetingItemCard` / `meeting-batch-transfer-button.tsx`（client） | 页面（server）传 `canTransfer` prop 或条件渲染 |
| 4 | 需求详情"带入 API 设计器"按钮（05） | 需求详情页内 | 随需求模块整体 gate（入口 2 覆盖） |

**注入点**：`src/app/dashboard/layout.tsx` 是 Server Component，已在此查 projects 列表——在同一处顺带查 profiles.feature_flags 即可，一次查询、整棵树共享。

**设计决策（建议，单一 flag）**：Phase 1 新功能共用一个模块级 flag key `requirement_hub`（jsonb 天然支持未来拆细粒度）。理由：灰度节奏是"先白名单，第 2 周全量"，一个开关足够；按功能拆 key 增加配置面无收益。**07 的分组视图切换（?view=grouped）不 gate**：纯只读展示，非白名单用户没有 source_requirement_id 数据，分组视图自然全部落在"未归属"，无害。

#### 2.4 server action 二次校验点（防绕过，共 7 个写操作）

| Action | 文件 | gate 范围 |
|---|---|---|
| createRequirement / updateRequirement / deleteRequirement / confirmRequirement / listRequirements | `src/app/dashboard/requirements/actions.ts`（03/04 新增） | 整个 action gate，flag off 返回 `{ok:false, error:"该功能尚未开放"}` |
| transferMeetingItemsToFeedback | `src/app/dashboard/feedback/actions.ts` 第 191 行（06 新增） | 整个 action gate |
| createApiDraft 的 `sourceRequirementId` 路径 | `src/app/dashboard/api-designer/actions.ts` 第 19 行（05 接线） | **只 gate 05 的参数路径**：flag off 且入参带 sourceRequirementId 时拒绝；不带该参数的 v1 原有用法不 gate |

注意 `regenerateApiDraft` / `saveVersion` / feedback 的其他 action 都是 v1 原有功能，**不 gate**。

#### 2.5 K-1/K-2/K-3/K-5 现状（checklist 第 4 项的验证对象）

定义在 `docs/v2统一技术方案.md` 技术债表：

| 项 | 内容 | 现状（已核实） |
|---|---|---|
| K-1 | DB 允许空项目名 | **已修**：007 迁移含 `projects_name_not_blank` check 约束（第 57 行），08 已推远程 DB → 09 只需记录"已生效" |
| K-2 | PostHog key 未配置 | Vercel 环境变量人工配置项，agent 无法代做 → 产出核对步骤 |
| K-3 | Google OAuth 未配置 | Supabase 控制台人工项（非阻塞）→ 产出核对步骤 |
| K-5 | Vercel 未连接部署 | 需先 push/merge 才能让生产拿到 Phase 1 代码 → **属用户决策，09 产出"上线操作手册"**（见 2.7） |

上线 checklist 原文：`docs/上线checklist.md`（v1 版，8 大节；09 报告引用它并更新当前数字，**不改这份历史文档**）。

#### 2.6 08 回归基线（09 改动后不得回退）

- 纯函数单测：**211/211** 绿（vitest）
- 集成测试：**25/25** 绿（`node scripts/integration-check.mjs`，v1 RLS 10 项 + Phase 1 新增；需 `.env.local` 三键 + 测试用户 A/B）
- AI 评测：**5/5** PASS（`DASHSCOPE_API_KEY=xxx node scripts/run-eval.mjs`）
- 详见 `.scratch/phase1-requirement-hub/issues/08-回归报告.md`

09 加了 gate 之后**必须重跑**这四项（灰度代码是 additive，但布局/页面改动可能牵连渲染）。

### 三、实施范围（in scope）

1. **纯函数 seam（TDD 先写测试）**：`src/lib/feature-flags.ts` 导出
   - `parseFeatureFlags(raw: unknown): { requirementHub: boolean }`——防御性解析 jsonb：null/undefined/非对象/数组 → `{requirementHub:false}`；`requirement_hub === true` 才开；其他 key 忽略
   - 对应测试 `src/lib/__tests__/feature-flags.test.ts`（vitest，风格参照 `requirements.test.ts`）
2. **类型补齐**：`src/types/database.ts` 增加 `Profile` 接口（含 `feature_flags: Record<string, unknown>`）
3. **读取 helper**：`loadFeatureFlags(client)`（接收 supabase client 参数，server client 与 action client 都能传）→ 内部 `from("profiles").select("feature_flags").eq("id", user.id)` + parseFeatureFlags；查不到行按全关处理
4. **前端 gate**：layout 查 flags → DashboardShell → Sidebar 过滤"需求"入口；requirements 页面 server 端 gate；会议转入按钮 prop gate
5. **action gate**：2.4 表中 7 处二次校验
6. **白名单 SQL**：`scripts/whitelist-requirement-hub.sql`，内容 `update profiles set feature_flags = '{"requirement_hub": true}'::jsonb where email in ('<白名单邮箱>');`——邮箱占位由用户填，通过 Supabase Dashboard SQL 编辑器执行；09 会话内可用测试账号实际执行一次验证 flag on 链路
7. **checklist 本地验证**：`npm run typecheck`（0 错误）+ `npm test`（含新测试全绿）+ `node scripts/integration-check.mjs`（25/25）+ `node scripts/run-eval.mjs`（5/5）+ `npm run build`（成功）
8. **上线就绪报告**：`.scratch/phase1-requirement-hub/issues/09-上线就绪报告.md`，含：灰度机制说明（flag key/入口清单/action 清单）、checklist 验证结果（命令+输出摘要）、K-1~K-5 状态、上线操作手册（见 2.7）
9. **工单收尾**：5 个 checklist 勾完，Status 改 done

#### 2.7 上线操作手册（09 的交付物之一，给用户执行）

报告里写清合并顺序与部署步骤（**agent 只写不做**）：

```
1. git push 依次：feat/03 → feat/04 → feat/08-regression → feat/09-feature-flag-release
   （03 先于 04，否则 04 的 PR diff 会带上 03+#02 的 commit；
    feat/08-regression 已含 05/06/07，无需单独推这三条）
2. 按 PR 顺序合并：03 → 04 → feat/08-regression → feat/09-feature-flag-release → main
3. Vercel 自动部署生产；核对环境变量：NEXT_PUBLIC_SUPABASE_* / SUPABASE_SERVICE_ROLE_KEY / DASHSCOPE_API_KEY / NEXT_PUBLIC_POSTHOG_KEY（K-2）
4. Supabase 控制台配置 Google OAuth provider（K-3，非阻塞）
5. 执行 whitelist SQL 开白名单 → 灰度验证 → 第 2 周全量（feature_flags 全量置 true 或代码侧去掉 gate）
6. 回滚预案：Vercel 即时回滚上一版本；灰度回滚 = 清空白名单 flag
```

### 四、不在范围内（out of scope）

- ❌ **不做 git push / 不开 PR / 不合并 main**——沿用 01-08 约定，上线操作留给用户（手册见 2.7）
- ❌ **不在 Vercel/Supabase 控制台做实际配置**（K-2/K-3/K-5 的人工部分）
- ❌ **不改 v1 功能行为**：createApiDraft 不带 sourceRequirementId 的路径、regenerate/saveVersion、feedback 其他 action 均 v1 原有，不 gate 不改
- ❌ **不做细粒度 per-feature flag**（单一 requirement_hub，理由见 2.3）
- ❌ **不 gate 07 分组视图**（只读无害，理由见 2.3）
- ❌ **不写 server action 单测**（action 层不单测，靠集成测试把关——与 03/08 一致约定）
- ❌ **不改 docs/上线checklist.md 历史文档**（09 报告引用并更新数字）
- ❌ **不做 K-4**（大音频断点续查，P2 范围）

### 五、验收门槛（全部满足才算 done）

- [ ] `feat/09-feature-flag-release` 从 feat/08-regression 切出，`npm run typecheck` 绿
- [ ] `parseFeatureFlags` 单测覆盖：null/undefined/数组/字符串/空对象/`requirement_hub:true`/`requirement_hub:false`/其他 key
- [ ] **flag off（默认用户）**：侧栏无"需求"入口；直链 /dashboard/requirements 显示"功能尚未开放"；调 7 个 gated action 均返回 `{ok:false, error:"该功能尚未开放"}`（防绕过验证：可用集成脚本模式或临时 node 脚本以 anon 会话直调验证思路记录在报告）
- [ ] **flag on（白名单账号）**：需求模块全链路可用（建需求→confirm→带入 API→会议条目转入）
- [ ] whitelist SQL 文件就位且已对测试账号实际执行
- [ ] 四项回归全绿：typecheck 0 错误 / vitest 全绿（211+新增）/ integration-check 25/25 / run-eval 5/5；`npm run build` 通过
- [ ] `09-上线就绪报告.md` 完整（含上线操作手册）
- [ ] 工单 5 个 checklist 全 `[x]`，Status 改 done，附 Branch + Note

### 六、收尾约定（与 03-08 一致）

完成后在 `09-Phase1灰度开关与上线就绪.md` 顶部追加：

```
**Status:** done
**Branch:** feat/09-feature-flag-release (commit <hash>)
**Note:** 分支未 push——按约定只 commit 到当前分支，未做 git push，也未开 PR。合并时需先合 03（feat/03-requirement-crud-filter）再合 04，再合 feat/08-regression（已含 05/06/07），最后合本分支。上线操作（push/PR/Vercel/白名单 SQL）见 09-上线就绪报告.md 的操作手册，由用户执行。至此 Phase 1 九工单全部完成。
```

### 七、工作方式建议（切片）

1. **切片 1：纯函数 seam（TDD）**（40 min）— 先写 `feature-flags.test.ts` 失败用例，再实现 `parseFeatureFlags` 转绿
2. **切片 2：读取链路**（40 min）— Profile 类型 + `loadFeatureFlags` + layout 注入 + sidebar 过滤 + requirements 页面 gate
3. **切片 3：action 二次校验**（40 min）— 7 处 gate，每处跑 typecheck
4. **切片 4：会议转入按钮 gate + 白名单 SQL**（30 min）— prop 传递 + SQL 文件 + 对测试账号执行
5. **切片 5：flag off/on 双向手验**（30 min）— 默认账号验证入口消失 + 直链占位；白名单账号验证全链路
6. **切片 6：四项回归 + build**（40 min）— typecheck / vitest / integration / eval / build，记录输出
7. **切片 7：就绪报告 + 工单收尾**（30 min）— 写报告（含操作手册），勾 checklist，改 Status

### 八、预读文档（实施前应熟悉）

- `.scratch/phase1-requirement-hub/issues/09-Phase1灰度开关与上线就绪.md`（本工单原文）
- `.scratch/phase1-requirement-hub/issues/08-回归报告.md`（回归基线与测试基建用法）
- `docs/上线checklist.md`（v1 上线 checklist 原文）
- `docs/v2统一技术方案.md`（K-1~K-5 技术债定义，grep "K-1"）
- `src/components/dashboard/sidebar.tsx` + `src/app/dashboard/layout.tsx`（gate 注入点）
- `src/app/dashboard/requirements/actions.ts` + `feedback/actions.ts` + `api-designer/actions.ts`（7 处 gate 落点）
- `src/lib/__tests__/requirements.test.ts`（vitest 风格锚点）
- `CONTEXT.md`（领域上下文）

## === 冷启动提示结束 ===

---

## 附：09 在 Phase 1 拓扑中的位置

```
main(01+02) → 03 → 04 → ┬─ 05 → 07 ──┐
                         └─ 06 ───────┴─→ 08（集成分支）→ 09（灰度+上线就绪）
```

09 完成即 Phase 1 全部九工单收口。上线操作（push/merge/deploy/白名单）由用户按报告手册执行。
